<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/confirmation_dialog_background"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/tv_dialog_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Photo Filters"
        android:textSize="20sp"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingTop="18dp"
        android:paddingBottom="18dp"
        android:background="#000000"
        android:textColor="#ffffff"
        android:gravity="center_vertical"/>
    <TextView
        android:id="@+id/tv_dialog_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tv_dialog_title"
        android:text="Make the posts more interesting with Campus Photo Filters.\nClick on the camera icon at the bottom to continue."
        android:textSize="16sp"
        android:padding="16dp"
        android:textColor="#000000"
        android:alpha="0.7"
        android:gravity="center_vertical"/>


        <TextView
            android:id="@+id/btn_okay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:clickable="true"
            android:layout_below="@+id/tv_dialog_info"
            android:background="#ebebeb"
            android:text="OKAY"
            android:textColor="#000000"
            android:alpha="0.87"
            android:gravity="center"/>

</RelativeLayout>